cable_sine_ref_node:
  ros__parameters:
    freq_hz:      0.2    # oscillation frequency (Hz)
    amplitude:    0.15    # peak displacement (m), retraction convention: positive = retract
    poll_rate_hz: 100.0
